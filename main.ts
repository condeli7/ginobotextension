input.onButtonPressed(Button.A, function on_button_pressed_a() {
    
})
let testVar = 0
ginobot.move(MoveDirection.Forward, Threshold.Low)
ginobot.move_duration(MoveDirection.Forward, Threshold.Low, Math.min(0, 0))
